import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Media & Latar Belakang",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
